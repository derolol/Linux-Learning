# 文件

+ 快su检索（定位） 分散存储（充分利用存储空间）

+ 

   ALU
    |
  CACHE (LINE)
    |
  MEMORY（PAGE）
    |
    HD（BLOCK）

## 2. Saving way

+ continue saving -> take continue space.
+ link saving -> every block record next pointer that the block no.
+ index saving -> index table continue + block can be not continue.

  + direct index -> saving data adress
  + first level index -> saving adress of the other index table
  + second level index
